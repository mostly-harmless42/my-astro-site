// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site : 'https://mostly-harmless42.github.io',
    base: '/my-astro-site'
});
